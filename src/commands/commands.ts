/* global Office */

Office.onReady(() => {
  // The commands.ts file is loaded when the add-in starts
  // The ribbon button actions are defined in the manifest and handled by the taskpane
});

// This file is required for the manifest but the actual functionality
// is handled in the taskpane since we're using ShowTaskpane action